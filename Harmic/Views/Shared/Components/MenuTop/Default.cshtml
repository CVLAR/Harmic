@model IEnumerable<Harmic.Models.TbMenu>
@foreach(var item in Model.Where(i=>i.Levels == 1))
{
    <li calss="drop-holder">
        <a href="/@item.Alias">
         @item.Title 
        </a>
    </li>
}
